import React from 'react'

const KafkaConnect = () => {
    return (
        <div style={{width: '36em', maxWidth: '100%'}}>
            <h4>Kafka Connect</h4>
            <div style={{opacity: .6}}>
                <p>Kafka Connect, an open source component of Apache Kafka®, is a framework for connecting Kafka with
                    external systems such as databases, key-value stores, search indexes, and file systems. Using Kafka
                    Connect, you can deploy connector implementations for common data sources and sinks to move data
                    into and out of Kafka.</p>
            </div>
        </div>
    )
}

export default KafkaConnect