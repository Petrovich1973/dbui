version: '3'
services:

  web:
    build: ./web
    networks:
      - networks-databases
    ports:
      - "8080:80"

  api:
    build: ./api
    restart: "always"
    networks:
      - networks-databases
    ports:
      - "5000:5000"

networks:
  networks-databases: