const project = 'Databases UI'
const version = "1.0"

const localStorageKey = `Databases_UI_${version}`

// API base url
const api_base_url = `${location.host}/api`

export {project, version, localStorageKey, api_base_url}